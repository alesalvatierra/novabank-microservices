server:
  port: 8082
spring:
  application:
    name: cuenta-service
  datasource:
    url: jdbc:postgresql://localhost:5432/novabank_cuentas
    username: postgres
    password: "745507"
  jpa:
    hibernate:
      ddl-auto: update
