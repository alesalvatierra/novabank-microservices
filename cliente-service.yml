server:
  port: 8081
spring:
  application:
    name: cliente-service
  datasource:
    url: jdbc:postgresql://localhost:5432/novabank_clientes
    username: postgres
    password: "745507"
  jpa:
    hibernate:
      ddl-auto: update
